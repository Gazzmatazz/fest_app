<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <%-# a partial to render a single user (ref: views/users/_user.html.erb) -%>
</ul>

<%= will_paginate %>