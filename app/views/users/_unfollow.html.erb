<%-# A form for unfollowing a user (using Ajax, remote: true)  -%>

<%= form_for(current_user.relationships.find_by_followed_id(@user),
             html: { method: :delete },
             remote: true) do |f| %>

  <%= f.submit "Unfollow", class: "btn btn-large" %>

<% end %>

<%-# this form only displays a button to send request -%>
<%-# finds the existing relationship, and sends a DELETE request -%>