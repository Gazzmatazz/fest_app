<!DOCTYPE html>
<html>

<head>  
  <%-# full_title method defined in application_helper.rb -%>
  <title><%= full_title(yield(:title)) %></title>
  
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%-# To deal with older versions of IE don't support html5: -%>
  <%= render 'layouts/shim' %>    
</head>

<body>

<%-# Render HEADER TAG with app logo and navbar links: -%>
	<%= render 'layouts/header' %>

  <div class="container">

      <%-# Flash variable for displaying messages with CSS based on the alert type/key, e.g. flash[:error] used to indicate a failed signin attempt.e.g2. flash[:success] for successful sign-up -%>
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

      <%= yield %>

      <%-# Partial file to render FOOTER TAG: -%>
      <%= render 'layouts/footer' %>

      <%-# the debug method displays controller and action info on each page (only displayed if in development environment)-%>
      <%= debug(params) if Rails.env.development? %>

  </div>

</body>

</html>
